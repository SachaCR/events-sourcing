const projection = require('./src/projection');
const applyEvent = require('./src/applyEvent');
const createEvent = require('./src/createEvent');
const revertEvent = require('./src/revertEvent');

module.exports = {
  projection,
  applyEvent,
  createEvent,
  revertEvent,
};
